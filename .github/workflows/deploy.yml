# ⚠️ ОТКЛЮЧЕНО: Используется Vercel для деплоя
# Этот workflow больше не используется, так как проект деплоится на Vercel
# 
# Для полного отключения GitHub Pages:
# 1. Перейти: https://github.com/Dmitry-Kuzmin/sdadim-dgt-prep/settings/pages
# 2. В разделе "Source" выбрать "None"
# 3. Нажать "Save"
#
# Или можно удалить этот файл полностью

# name: Deploy to GitHub Pages
#
# on:
#   push:
#     branches: [ main, master, feature/premium-race-game ]
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pages: write
#   id-token: write
#
# concurrency:
#   group: "pages"
#   cancel-in-progress: false
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#       # ... остальной код закомментирован
