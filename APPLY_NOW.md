# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ - Применить немедленно!

## ❌ Проблема
Изменение политик вызвало множество ошибок:
- "mismatch between server and client bindings"
- Edge Function возвращает 500 ошибку
- Игра не запускается

## ✅ Решение

### Быстрое применение (2 минуты):

1. **Откройте SQL Editor:**
   ```
   https://supabase.com/dashboard/project/ijijcrucqqnnjbkclqhb/sql/new
   ```

2. **Скопируйте ВСЁ содержимое файла `URGENT_FIX.sql`**

3. **Вставьте в SQL Editor**

4. **Нажмите Run** (или `Ctrl+Enter` / `Cmd+Enter`)

5. **Проверьте результат** - должны быть сообщения:
   - `✅ Table duel_notifications is in supabase_realtime publication`

6. **Перезагрузите страницу** в браузере

7. **Проверьте игру** - должна запускаться без ошибок

---

## 📋 Что делает миграция:

1. ✅ Восстанавливает политику для profiles (разрешает чтение всех профилей)
2. ✅ Исправляет RLS политику для уведомлений (использует функцию вместо подзапроса)
3. ✅ Включает Realtime для таблицы уведомлений

---

## 🔍 После применения проверьте:

1. **Консоль браузера** (F12):
   - Должны исчезнуть ошибки "mismatch between server and client bindings"
   - Должно появиться: `[Notifications] ✅ Successfully subscribed to notifications`

2. **Игра должна запускаться** без ошибки 500

3. **Имя соперника** должно отображаться вместо "Соперник"

---

## ⚠️ Если миграция не применяется:

1. Проверьте, что вы используете правильный проект
2. Убедитесь, что нет синтаксических ошибок
3. Проверьте логи в Supabase Dashboard → Logs

---

## 📄 Файл для применения:
**`URGENT_FIX.sql`** - содержит все необходимые исправления


