# 🚨 ПРАВИЛА ДЛЯ CURSOR ПРИ РАБОТЕ С LOVABLE ПРОЕКТОМ

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Lovable имеет специальную структуру проекта и требует соблюдения строгих правил.**

---

## ❌ ЗАПРЕЩЕНО ИЗМЕНЯТЬ БЕЗ ЯВНОЙ ПРОСЬБЫ ПОЛЬЗОВАТЕЛЯ

### 🔴 Конфигурационные файлы (НЕ ТРОГАТЬ!)

1. **`package.json`**
   - ❌ НЕ удалять зависимости
   - ❌ НЕ изменять версии пакетов
   - ❌ НЕ удалять скрипты
   - ❌ НЕ изменять структуру файла
   - ✅ Только если пользователь ЯВНО попросил

2. **`vite.config.ts`**
   - ❌ НЕ удалять импорт `lovable-tagger`
   - ❌ НЕ удалять `componentTagger()` из plugins
   - ❌ НЕ изменять конфигурацию без запроса
   - ✅ Только если пользователь ЯВНО попросил

3. **`tsconfig.json` / `tsconfig.app.json` / `tsconfig.node.json`**
   - ❌ НЕ изменять конфигурацию TypeScript
   - ✅ Только если пользователь ЯВНО попросил

4. **`tailwind.config.ts`**
   - ❌ НЕ изменять конфигурацию Tailwind
   - ✅ Только если пользователь ЯВНО попросил

5. **`postcss.config.js`**
   - ❌ НЕ изменять конфигурацию PostCSS
   - ✅ Только если пользователь ЯВНО попросил

6. **`eslint.config.js`**
   - ❌ НЕ изменять конфигурацию ESLint
   - ✅ Только если пользователь ЯВНО попросил

7. **`.gitignore`**
   - ❌ НЕ изменять без необходимости
   - ✅ Только если пользователь ЯВНО попросил

8. **`index.html`**
   - ❌ НЕ изменять структуру HTML
   - ✅ Только если пользователь ЯВНО попросил

---

## ✅ РАЗРЕШЕНО ИЗМЕНЯТЬ

### 📁 Компоненты и бизнес-логика

1. **`src/components/`** - все компоненты (.tsx файлы)
   - ✅ Изменять логику компонентов
   - ✅ Добавлять новые компоненты
   - ✅ Улучшать UI/UX

2. **`src/pages/`** - страницы приложения (.tsx файлы)
   - ✅ Изменять страницы
   - ✅ Добавлять новые страницы

3. **`src/hooks/`** - кастомные хуки (.ts, .tsx файлы)
   - ✅ Изменять хуки
   - ✅ Добавлять новые хуки

4. **`src/contexts/`** - контексты React (.tsx файлы)
   - ✅ Изменять контексты
   - ✅ Добавлять новые контексты

5. **`src/lib/`** - утилиты и библиотеки (.ts файлы)
   - ✅ Изменять утилиты
   - ✅ Добавлять новые утилиты

6. **`src/utils/`** - вспомогательные функции (.ts файлы)
   - ✅ Изменять утилиты
   - ✅ Добавлять новые утилиты

7. **`src/types/`** - типы TypeScript (.d.ts, .ts файлы)
   - ✅ Изменять типы
   - ✅ Добавлять новые типы

8. **`src/integrations/`** - интеграции (например, Supabase)
   - ✅ Изменять интеграции
   - ⚠️ Будьте осторожны с клиентами

9. **`src/core/`** - основная логика (.ts файлы)
   - ✅ Изменять основную логику

10. **`src/data/`** - данные (.csv, .json файлы)
    - ✅ Изменять данные

11. **`supabase/functions/`** - Edge Functions
    - ✅ Изменять функции
    - ✅ Добавлять новые функции

12. **`supabase/migrations/`** - миграции базы данных
    - ✅ Создавать новые миграции
    - ❌ НЕ изменять существующие миграции без явной просьбы

---

## 🔑 КРИТИЧЕСКИЕ ЗАВИСИМОСТИ LOVABLE

### Обязательные пакеты (НЕ УДАЛЯТЬ!)

1. **`lovable-tagger`** - КРИТИЧНО для работы Lovable
   - Используется в `vite.config.ts`
   - Обязателен для режима разработки
   - Должен быть в `devDependencies`

2. **`@vitejs/plugin-react-swc`** - плагин Vite для React
   - Используется в `vite.config.ts`

3. **Все `@radix-ui/*` компоненты** - UI библиотека
   - Используются в компонентах
   - Не удалять без необходимости

4. **`lucide-react`** - иконки
   - Используется в компонентах
   - Не удалять

---

## 📋 ЧЕКЛИСТ ПЕРЕД ИЗМЕНЕНИЕМ ФАЙЛА

Перед изменением файла спросите себя:

- [ ] Это конфигурационный файл? (package.json, vite.config.ts, tsconfig.json и т.д.)
  - Если ДА → **НЕ ИЗМЕНЯТЬ** без явной просьбы пользователя
- [ ] Это файл в `src/` или `supabase/functions/`?
  - Если ДА → ✅ Можно изменять
- [ ] Это файл в корне проекта?
  - Если ДА → **ПРОВЕРИТЬ** - это конфигурация?
- [ ] Нужно ли удалить зависимость?
  - Если ДА → **СПРОСИТЬ** пользователя сначала
- [ ] Нужно ли изменить структуру проекта?
  - Если ДА → **СПРОСИТЬ** пользователя сначала

---

## 🎯 ПРАВИЛО №1: ЕСЛИ СОМНЕВАЕШЬСЯ - СПРОСИ

Если не уверены, можно ли изменять файл:
1. **СПРОСИТЕ** пользователя
2. **ОБЪЯСНИТЕ**, что хотите изменить и зачем
3. **ПОДОЖДИТЕ** подтверждения
4. **ТОЛЬКО ПОТОМ** вносите изменения

---

## 🚫 ЧТО ПРОИЗОШЛО РАНЬШЕ (НЕ ДЕЛАТЬ!)

### ❌ Ошибка 1: Удаление зависимостей из package.json
- Было удалено 17+ пакетов
- Проект перестал собираться
- Lovable не смог запустить проект

### ❌ Ошибка 2: Изменение vite.config.ts
- Удален импорт `lovable-tagger`
- Удален `componentTagger()` из plugins
- Нарушена работа Lovable

### ❌ Ошибка 3: Изменение конфигурационных файлов без запроса
- Изменены файлы без явной просьбы пользователя
- Привело к проблемам со сборкой

---

## ✅ ПРАВИЛЬНЫЙ ПОДХОД

### Пример 1: Изменение компонента
```typescript
// ✅ МОЖНО: src/components/MyComponent.tsx
export function MyComponent() {
  // Изменяем логику компонента
}
```

### Пример 2: Изменение конфигурации
```typescript
// ❌ НЕЛЬЗЯ: vite.config.ts
// НЕ изменять без явной просьбы!

// ✅ МОЖНО: Спросить пользователя
// "Пользователь, нужно изменить vite.config.ts для [причина]. Можно?"
```

### Пример 3: Добавление зависимости
```typescript
// ❌ НЕЛЬЗЯ: Автоматически добавлять в package.json
// ✅ МОЖНО: Спросить пользователя
// "Пользователь, для [функция] нужна зависимость [пакет]. Добавить?"
```

---

## 📝 ШАБЛОН ДЛЯ СПРОСА ПОЛЬЗОВАТЕЛЯ

Если нужно изменить конфигурационный файл:

```
⚠️ ВНИМАНИЕ: Для [описание изменения] нужно изменить [файл].

Изменения:
- [что будет изменено]

Последствия:
- [что может произойти]

Можно внести эти изменения? Или есть другой способ?
```

---

## 🚀 ДЕПЛОЙ НА VERCEL

### ⚠️ КРИТИЧЕСКИ ВАЖНО

**Автодеплой через Git отключен!** После каждого коммита нужно **ВСЕГДА** выполнять деплой вручную.

### ✅ ОБЯЗАТЕЛЬНО после каждого коммита:

1. **Выполнить деплой на production:**
   ```bash
   vercel --prod --yes
   ```

2. **Правила:**
   - ✅ ВСЕГДА выполняй `vercel --prod --yes` после `git commit` и `git push`
   - ✅ НЕ жди подтверждения от пользователя - делай автоматически
   - ✅ Если деплой не удался - исправь ошибки и повтори
   - ✅ НЕ пропускай деплой, даже если изменения кажутся незначительными

3. **Исключения:**
   - ❌ НЕ деплоить, если пользователь ЯВНО попросил не деплоить
   - ❌ НЕ деплоить, если есть критические ошибки сборки (исправить сначала)

### 📝 Пример workflow:

```bash
# 1. Внес изменения
# 2. Закоммитил
git add .
git commit -m "fix: исправлена ошибка"
git push

# 3. ВСЕГДА деплою на production
vercel --prod --yes
```

## 🔄 СВЯЗЬ С GIT_WORKFLOW.md

Эти правила работают вместе с правилами из `GIT_WORKFLOW.md`:
- Сначала синхронизируйтесь с Lovable (`git pull --rebase`)
- Работайте только с разрешенными файлами
- Коммитьте изменения
- Отправляйте в репозиторий
- **ВСЕГДА деплоить на Vercel после push**

---

## 💡 ПОМНИТЕ

**Cursor должен работать ТОЛЬКО с бизнес-логикой и компонентами, НЕ трогая инфраструктурные файлы!**

---

## 🎨 ЕДИНАЯ СИСТЕМА МОДАЛОК

### ✅ ОБЯЗАТЕЛЬНО для всех модалок:

**ВСЕ модалки ДОЛЖНЫ использовать единую систему skeleton анимации!**

1. **Импорты**:
```typescript
import { ModalSkeleton } from '@/components/ui/modal-skeleton';
import { getDialogContentClasses, getSheetContentClasses } from '@/lib/modal-config';
import { useIsMobile } from '@/hooks/use-mobile';
```

2. **Структура**:
```typescript
export function MyModal({ open, onOpenChange }: MyModalProps) {
  const isMobile = useIsMobile();
  const [loading, setLoading] = useState(true);

  // Контент модалки с skeleton
  const ModalContent = () => {
    if (loading) {
      return <ModalSkeleton variant="default" />; // или "shop", "duelPass", "profile"
    }
    return <>{/* Ваш контент */}</>;
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className={getDialogContentClasses('default', isMobile)}>
        <div className="flex-1 overflow-y-auto">
          <ModalContent />
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

3. **Правила**:
   - ✅ ВСЕГДА используйте `ModalContent` функцию для условного рендеринга skeleton
   - ✅ ВСЕГДА используйте `getDialogContentClasses()` или `getSheetContentClasses()` для адаптивной высоты (`max-h`)
   - ✅ ВСЕГДА оборачивайте контент в `<div className="flex-1 overflow-y-auto">`
   - ✅ НИКОГДА не используйте ранний `return` при loading - это изменит размер модалки
   - ✅ НИКОГДА не задавайте `h-[...vh]` на DialogContent — только `max-h` и внутренний скролл

4. **Варианты skeleton**:
   - `default` - базовый skeleton
   - `shop` - для магазина (табы, карточки)
   - `duelPass` - для Duel Pass (прогресс, таблица)
   - `profile` - для профиля (аватар, статистика)

5. **Документация**: См. `MODAL_SYSTEM.md` для подробностей

**ВАЖНО**: При создании НОВОЙ модалки - ВСЕГДА используйте эту систему!

---

## 🌍 ПЕРЕВОДЫ - ОБЯЗАТЕЛЬНОЕ ТРЕБОВАНИЕ

### ⚠️ КРИТИЧЕСКИ ВАЖНО

**ВСЕ новые UI блоки, компоненты и тексты ДОЛЖНЫ быть переведены автоматически при создании!**

### ✅ Правила перевода:

1. **При создании нового компонента с текстом:**
   - ✅ Создай файл переводов в `src/translations/componentName.ts`
   - ✅ Добавь переводы для всех языков (ru, es, en)
   - ✅ Используй `useLanguage()` hook в компоненте
   - ✅ НЕ используй хардкод текста в JSX

2. **Структура файла переводов:**
   ```typescript
   import { Language } from "@/contexts/LanguageContext";
   
   export const componentTranslations: Record<Language, ComponentTranslations> = {
     ru: { key: "Русский" },
     es: { key: "Español" },
     en: { key: "English" },
   };
   ```

3. **Использование в компоненте:**
   ```typescript
   const { language } = useLanguage();
   const t = componentTranslations[language];
   return <span>{t.key}</span>; // ✅ Правильно
   // return <span>Текст</span>; // ❌ Неправильно
   ```

4. **Что переводить:**
   - ✅ Все тексты в UI (кнопки, заголовки, описания)
   - ✅ Сообщения об ошибках и уведомления
   - ✅ Плейсхолдеры в формах
   - ✅ Подсказки и хелп-тексты

5. **Чеклист:**
   - [ ] Создан файл переводов
   - [ ] Добавлены переводы для ru, es, en
   - [ ] Компонент использует `useLanguage()`
   - [ ] Нет хардкода текста
   - [ ] Проверена работа переключения языков

**Подробности:** См. `TRANSLATION_RULES.md`

---

## 🚀 ИННОВАЦИИ И СОВРЕМЕННЫЕ РЕШЕНИЯ

### ⚠️ ОБЯЗАТЕЛЬНОЕ ТРЕБОВАНИЕ

**Cursor ДОЛЖЕН всегда предлагать современные технологические решения и инновационные идеи!**

Пользователь не всегда знает о современных технологиях - задача Cursor:
- ✅ Предлагать несколько вариантов решения (базовый, продвинутый, инновационный)
- ✅ Объяснять простым языком без сложных терминов
- ✅ Показывать преимущества и недостатки каждого варианта
- ✅ Рекомендовать лучший вариант с обоснованием

### ✅ Правила работы с инновациями:

1. **Всегда предлагай несколько вариантов:**
   - ✅ Базовый вариант (простой, быстрый)
   - ✅ Продвинутый вариант (современный, с фичами)
   - ✅ Инновационный вариант (экспериментальный, высокотехнологичный)

2. **Объясняй простым языком:**
   - ✅ Используй аналогии и примеры
   - ✅ Избегай сложных технических терминов без объяснения
   - ✅ Показывай преимущества и недостатки каждого варианта

3. **Предлагай современные технологии:**
   - ✅ AI/ML решения (где уместно)
   - ✅ Автоматизация процессов
   - ✅ Аналитика и трекинг (UTM, pixels, webhooks)
   - ✅ Интеграции с внешними сервисами
   - ✅ Оптимизация производительности
   - ✅ UX/UI улучшения (QR-коды, виджеты, геймификация)

4. **При создании новой фичи:**
   - ✅ Предложи 2-3 альтернативных подхода
   - ✅ Объясни плюсы и минусы каждого
   - ✅ Рекомендуй лучший вариант с обоснованием
   - ✅ Предложи дополнительные улучшения

5. **Регулярно предлагай улучшения:**
   - ✅ При работе с кодом - предложи оптимизации
   - ✅ При создании фичи - предложи дополнительные возможности
   - ✅ При исправлении бага - предложи профилактику

### 📋 Примеры инновационных решений:

1. **Аналитика и трекинг:**
   - UTM-метки для отслеживания источников
   - Pixel-трекинг для конверсий
   - Heatmaps для анализа поведения

2. **Автоматизация:**
   - Webhook-уведомления
   - Email-последовательности
   - Автоматические отчеты

3. **UX/UI улучшения:**
   - QR-коды для быстрого доступа
   - Прогрессивная загрузка
   - Микро-анимации
   - Адаптивный дизайн

4. **Производительность:**
   - Кэширование данных
   - Ленивая загрузка
   - Оптимизация запросов

### 🎯 Чеклист при создании фичи:

- [ ] Предложено 2-3 варианта реализации
- [ ] Объяснены преимущества и недостатки
- [ ] Рекомендован лучший вариант
- [ ] Предложены дополнительные улучшения
- [ ] Учтены современные технологии
- [ ] Объяснение простым языком

**ПОМНИ:** Пользователь не всегда знает о современных решениях - твоя задача предложить их и объяснить простым языком!

---

---

## 🚀 ПРОИЗВОДИТЕЛЬНОСТЬ И ОПТИМИЗАЦИЯ (КРИТИЧЕСКИ ВАЖНО!)

### ⚠️ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА ПРОИЗВОДИТЕЛЬНОСТИ

**ВСЕ изменения кода ДОЛЖНЫ соответствовать правилам производительности!**

#### 📊 Метрики и лимиты:

1. **Запросы к Supabase:**
   - ❌ ЗАПРЕЩЕНО: Более 5 запросов к одной таблице в одном компоненте
   - ❌ ЗАПРЕЩЕНО: Прямые запросы в компонентах (без React Query)
   - ❌ ЗАПРЕЩЕНО: Последовательные запросы, которые можно объединить
   - ✅ ОБЯЗАТЕЛЬНО: Использовать React Query для ВСЕХ запросов
   - ✅ ОБЯЗАТЕЛЬНО: Использовать существующие хуки (useProfileData, usePremium)
   - ✅ ОБЯЗАТЕЛЬНО: Кэшировать данные минимум на 1 минуту

2. **Edge Functions:**
   - ❌ ЗАПРЕЩЕНО: Более 3 вызовов одной Edge Function в компоненте
   - ❌ ЗАПРЕЩЕНО: Вызовы без кэширования
   - ✅ ОБЯЗАТЕЛЬНО: Использовать React Query с staleTime >= 2 минуты

3. **Realtime подписки:**
   - ❌ ЗАПРЕЩЕНО: Множественные подписки на один канал
   - ✅ ОБЯЗАТЕЛЬНО: Проверять существующие подписки перед созданием

#### 🔍 Автоматические проверки перед коммитом:

```bash
# Проверить прямые запросы (должно быть 0)
grep -r "supabase\.from\|supabase\.functions\.invoke" src/components/ src/pages/ --include="*.tsx" | wc -l

# Проверить использование React Query
grep -r "useQuery\|useMutation" src/hooks/ | wc -l
```

#### 📝 Чеклист перед коммитом:

- [ ] Все запросы к Supabase используют React Query
- [ ] Нет прямых запросов в компонентах (только через хуки)
- [ ] Нет дублирующихся запросов к одной таблице
- [ ] Все Edge Functions вызываются через React Query
- [ ] Realtime подписки проверяются на дубликаты
- [ ] Кэширование настроено правильно (staleTime >= 1 минута)

**Подробности:** См. `PERFORMANCE_RULES.md` для полных правил и примеров.

---

---

## 🎨 СТАНДАРТ UI БИБЛИОТЕК (ОБЯЗАТЕЛЬНО!)

### ⚠️ КРИТИЧЕСКИ ВАЖНО

**ВСЕ новые модалки, попапы и уведомления ДОЛЖНЫ использовать новый стандарт библиотек!**

### 📚 Золотой Стек 2025 года

Для нашего стека (**React + Tailwind + Telegram Mini App**) используется следующий стек:

#### 1. **shadcn/ui + Radix UI** (База)

- ✅ **Dialog** (модалки) - `@radix-ui/react-dialog`
- ✅ **Drawer** (шторки) - `vaul` (используется под капотом shadcn Drawer)
- ✅ **Popover** - `@radix-ui/react-popover`
- ✅ **Alert Dialog** - `@radix-ui/react-alert-dialog`
- ✅ **Tooltip** - `@radix-ui/react-tooltip`

**Почему это топ:**
- Headless UI (логика без стилей)
- Полная доступность (a11y) из коробки
- Focus Trap, блокировка скролла, управление клавиатурой
- Полностью стилизуется через Tailwind

#### 2. **Vaul** (Мобильная шторка)

- ✅ Используется для **Drawer** компонента
- ✅ Имитирует нативное поведение iOS
- ✅ Можно тянуть, закрывать свайпом
- ✅ Ощущается как часть Telegram, а не веб-сайт

#### 3. **Framer Motion** (Анимации)

- ✅ Для плавных анимаций появления/исчезновения
- ✅ `AnimatePresence` для анимации удаления из DOM
- ✅ Используется для эффектов "зума" и "скольжения"

#### 4. **Sonner** (Уведомления/Toasts)

- ✅ Легковесная библиотека
- ✅ Премиум внешний вид
- ✅ Работает поверх всех модалок (правильный z-index)
- ✅ Настраивается через Tailwind

### ✅ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

#### 1. ResponsiveModal - Умный компонент (ЗОЛОТОЙ СТАНДАРТ)

**🏆 ВСЕ новые модалки ДОЛЖНЫ использовать `ResponsiveModal` с Vaul для мобильных!**

```tsx
import { ResponsiveModal } from "@/components/ui/responsive-modal";

export function MyModal({ open, onOpenChange }: MyModalProps) {
  return (
    <ResponsiveModal
      open={open}
      onOpenChange={onOpenChange}
      title="Заголовок"
      description="Описание (опционально)"
      snapPoints={[0.85, 1]} // Опционально: точки остановки для Vaul
    >
      {/* Ваш контент */}
    </ResponsiveModal>
  );
}
```

**Как работает:**
- На десктопе (>= 768px): **Dialog** (Radix UI) - центрированная модалка
- На мобильных (< 768px): **Drawer** (Vaul) - шторка снизу с нативным поведением iOS

**Почему Vaul - это Золотой стандарт для Telegram Mini App (2025):**

1. **Нативность (Native Feel)**: Пользователи Telegram привыкли смахивать шторки вниз пальцем. Vaul дает эту физику (drag & dismiss). Radix Sheet ощущается как «веб-сайт», а не как «приложение».

2. **Snap Points (Точки остановки)**: Шторка может открываться на 85% экрана (чтобы было видно фон), а если потянуть выше — на 100%. Vaul это умеет из коробки.

3. **Производительность**: Vaul лучше работает с клавиатурой и фокусом на мобильных.

4. **UX**: Плавное открытие, инерция, закрытие свайпом — ощущение "дорогого" приложения.

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- ❌ **НИКОГДА** не используйте `UnifiedModal` или `Sheet` из Radix UI для мобильных
- ✅ **ВСЕГДА** используйте `ResponsiveModal`, который автоматически использует Vaul на мобильных
- ✅ Для кастомных snap points передавайте `snapPoints={[0.85, 1]}` в `ResponsiveModal`

#### 2. Уведомления - ТОЛЬКО Sonner

**ВСЕ уведомления ДОЛЖНЫ использовать `toast` из `sonner`:**

```tsx
import { toast } from "@/components/ui/sonner";

// Успех
toast.success("Награда получена!", {
  description: "Вы получили 100 монет",
});

// Ошибка
toast.error("Ошибка оплаты", {
  description: "Недостаточно средств",
});

// Информация
toast.info("Новое обновление", {
  description: "Доступна новая функция",
});
```

**ЗАПРЕЩЕНО:**
- ❌ Использовать старую систему `use-toast` из `@radix-ui/react-toast`
- ❌ Создавать кастомные компоненты уведомлений
- ❌ Использовать `react-hot-toast` или другие библиотеки

#### 3. Премиум стиль (zinc-900)

**ВСЕ компоненты ДОЛЖНЫ использовать премиум стиль:**

- **Фон:** `bg-zinc-900` (НЕ `bg-background`, НЕ `bg-slate-950`)
- **Границы:** `border-zinc-800 border-white/10` (тонкие, премиум)
- **Текст:** 
  - Заголовки: `text-zinc-200`
  - Описания: `text-zinc-400`
  - Мелкий текст: `text-zinc-500`
- **Скругление:** `rounded-xl` для модалок, `rounded-full` для badges

#### 4. Структура компонентов

**Dialog (десктоп):**
```tsx
<Dialog open={open} onOpenChange={onOpenChange}>
  <DialogContent className="bg-zinc-900 border-zinc-800 border-white/10">
    <DialogHeader>
      <DialogTitle className="text-zinc-200">Заголовок</DialogTitle>
      <DialogDescription className="text-zinc-400">Описание</DialogDescription>
    </DialogHeader>
    {/* Контент */}
  </DialogContent>
</Dialog>
```

**Drawer (мобильные):**
```tsx
<Drawer open={open} onOpenChange={onOpenChange}>
  <DrawerContent className="bg-zinc-900 border-zinc-800 border-t-white/10">
    <DrawerHeader>
      <DrawerTitle className="text-zinc-200">Заголовок</DrawerTitle>
      <DrawerDescription className="text-zinc-400">Описание</DrawerDescription>
    </DrawerHeader>
    {/* Контент */}
  </DrawerContent>
</Drawer>
```

### 📋 Чеклист при создании новой модалки:

- [ ] Используется `ResponsiveModal` или `Dialog`/`Drawer` напрямую
- [ ] Применен премиум стиль (`bg-zinc-900`, `border-zinc-800`, `text-zinc-200/400`)
- [ ] Уведомления используют `toast` из `sonner`
- [ ] Проверена работа на мобильных (Drawer) и десктопе (Dialog)
- [ ] Добавлены анимации через Framer Motion (если нужно)

### 🚫 ЗАПРЕЩЕНО

- ❌ Использовать `react-modal` или другие старые библиотеки
- ❌ Использовать `Material UI` или `Ant Design` (тяжелые, убивают кастомный дизайн)
- ❌ Создавать кастомные модалки без использования Radix UI
- ❌ Использовать старую систему уведомлений (`use-toast`)

### 📦 Установленные зависимости

Все необходимые зависимости уже установлены:
- ✅ `@radix-ui/react-dialog` - Dialog
- ✅ `@radix-ui/react-popover` - Popover
- ✅ `@radix-ui/react-alert-dialog` - Alert Dialog
- ✅ `vaul` - Drawer (мобильная шторка)
- ✅ `framer-motion` - Анимации
- ✅ `sonner` - Уведомления
- ✅ `class-variance-authority` - Варианты компонентов
- ✅ `clsx` + `tailwind-merge` - Утилиты для классов

**НЕ нужно устанавливать дополнительные зависимости!**

---

**Последнее обновление:** 2025-12-01