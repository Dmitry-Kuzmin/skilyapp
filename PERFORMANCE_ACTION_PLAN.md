# Performance Action Plan (исправленный)

**Основан на**: Code review от Димы + PageSpeed Insights анализ  
**Дата**: 2025-12-05

## 🎯 Правильная интерпретация метрик

### Что значат цифры:

| Метрика | Значение | Интерпретация |
|---------|----------|---------------|
| **FCP 6.4s** | Poor | Первый видимый контент появляется поздно |
| **LCP 7.6s** | Poor | Главный контент появляется поздно |
| **TBT 130ms** | Good | **JS НЕ блокирует** - проблема в сети/загрузке ресурсов |
| **CLS 0** | Good | Нет визуальных сдвигов |
| **Speed Index 6.6s** | Poor | Визуально медленная загрузка |

### Ключевой вывод:
**TBT 130ms** означает что проблема **НЕ в JS execution**, а скорее в:
- Сетевой задержке доставки ресурсов
- Последовательной загрузке ресурсов
- Медленных API запросах (для LCP, не для FCP)

## ✅ Что проверить ПЕРВЫМ

### 1. Skeleton в production
**Вопрос**: Задеплоен ли skeleton в production?

**Проверка**:
```bash
# 1. Проверить production HTML
curl https://skilyapp.com/ | grep -A 20 "app-skeleton"

# 2. Проверить filmstrip в PageSpeed
# Открыть отчёт → Performance → Filmstrip
# Виден ли skeleton в первые секунды?

# 3. DevTools Performance
# Записать профиль загрузки → проверить когда появляется skeleton
```

**Если skeleton НЕ в prod**:
- Задеплоить изменения
- Запустить новый PageSpeed анализ
- Сравнить метрики

**Если skeleton в prod, но FCP всё равно 6.4s**:
- Проверить CSS - может быть скрыт через `display:none`
- Проверить что skeleton виден ДО загрузки JS
- Проверить Network waterfall

### 2. Network Waterfall анализ
**Цель**: Найти что реально блокирует FCP

**Как проверить**:
```bash
# Chrome DevTools:
1. Network tab → Throttling: Slow 4G
2. Reload страницу
3. Посмотреть waterfall:
   - Какие ресурсы грузятся до FCP?
   - Что блокирует рендеринг?
   - Какие запросы самые медленные?
```

**Что искать**:
- HTML загружается быстро? (TTFB)
- CSS блокирует? (render-blocking)
- JS chunks грузятся последовательно?
- Изображения блокируют?
- API запросы до FCP? (не должны быть)

### 3. Bundle Analysis
**Цель**: Понять размер и состав bundle

**Как запустить**:
```bash
npm run build:analyze
# Откроется визуализация bundle (stats.html)
```

**Что проверить**:
- Размер vendor chunk
- Размер index chunk
- Самые тяжёлые зависимости
- Unused JS (через Coverage в DevTools)

**Ожидаемые проблемы**:
- recharts (если используется только в админке)
- framer-motion (если используется везде)
- Большой vendor bundle (все node_modules вместе)

## 🔧 План действий (правильный порядок)

### Шаг 1: Диагностика (СЕЙЧАС)

1. ✅ **Проверить skeleton в production**
   - Задеплоен ли?
   - Виден ли в filmstrip?

2. ✅ **Network Waterfall**
   - Что блокирует FCP?
   - Какие ресурсы грузятся последовательно?

3. ✅ **Bundle Analysis**
   - Размеры chunks
   - Тяжёлые зависимости
   - Unused JS

### Шаг 2: Исправления (после диагностики)

#### Если skeleton НЕ в prod:
- Задеплоить изменения
- Перезапустить PageSpeed
- Сравнить метрики

#### Если проблема в Network:
- Preload критичных ресурсов (через Vite plugin)
- Preconnect к Supabase (уже есть)
- Оптимизация последовательной загрузки

#### Если проблема в Bundle:
- Code splitting (уже есть, но проверить)
- Lazy loading тяжёлых компонентов
- Удаление неиспользуемых зависимостей
- Разделение vendor на части

#### Если проблема в API (LCP):
- React Query prefetchQuery (не `<link rel="prefetch">`)
- Параллелизация запросов
- Агрессивное кэширование
- SSR для критичных данных (если возможно)

#### Если проблема в изображениях:
- WebP формат
- Lazy loading
- Responsive images (srcset)
- CDN для изображений

### Шаг 3: Оптимизация (после исправлений)

1. **Оптимизация изображений**
   - WebP конвертация
   - Lazy loading
   - Responsive images

2. **Preload критичных ресурсов**
   - Через Vite plugin (не вручную)
   - Только критичные chunks

3. **Оптимизация API**
   - Prefetch через React Query
   - Параллелизация
   - Кэширование

## ❌ Что НЕ делать

### 1. HTTP/2 Server Push
- ❌ Устарел, не рекомендуется
- ✅ Использовать `preload` вместо push

### 2. Service Worker для первой загрузки
- ❌ SW не ускоряет первую загрузку
- ✅ SW помогает только для повторных визитов

### 3. `<link rel="prefetch" href="/api/dashboard">`
- ❌ Не самый надёжный паттерн
- ✅ Использовать React Query `prefetchQuery` в JS

### 4. Preload с `*` в HTML
- ❌ `*` не подставится сам
- ✅ Использовать Vite plugin для автоматической подстановки

## 📊 Правильные выводы

### Что мы знаем точно:
1. ✅ **TBT 130ms** = JS не блокирует (проблема в сети/загрузке)
2. ✅ **CLS 0** = нет визуальных сдвигов
3. ✅ **FCP 6.4s** = первый контент появляется поздно
4. ✅ **LCP 7.6s** = главный контент появляется поздно

### Что мы НЕ знаем (нужно проверить):
1. ❓ Skeleton задеплоен в prod?
2. ❓ Виден ли skeleton в filmstrip?
3. ❓ Что блокирует FCP (Network waterfall)?
4. ❓ Размер bundle (bundle analysis)?
5. ❓ Unused JS (Coverage)?

### Что мы НЕ должны утверждать:
1. ❌ "Skeleton не помогает" - пока не проверили
2. ❌ "Supabase блокирует FCP" - FCP не должен ждать API
3. ❌ "Тяжёлый JS bundle" - при TBT 130ms это не главная проблема
4. ❌ "Блокирующие скрипты" - при TBT 130ms это не главная проблема

## 🎯 Реалистичные цели

### После исправлений (реалистично):

| Метрика | Сейчас | Цель | Улучшение |
|---------|--------|------|-----------|
| **FCP** | 6.4s | 2.0s | -68% |
| **LCP** | 7.6s | 3.0s | -60% |
| **Speed Index** | 6.6s | 4.0s | -39% |
| **Performance** | 59 | 75+ | +16 points |

### Условия достижения:
- Skeleton задеплоен и работает
- Bundle оптимизирован
- Изображения оптимизированы
- API запросы оптимизированы
- Preload критичных ресурсов

## 📝 Следующие шаги

1. **СЕЙЧАС**: Проверить skeleton в production
2. **СЕЙЧАС**: Network Waterfall анализ
3. **СЕЙЧАС**: Bundle analysis
4. **ПОТОМ**: Исправить найденные проблемы
5. **ПОТОМ**: Новый PageSpeed анализ
6. **ПОТОМ**: Сравнить метрики

---

**Важно**: Не делать выводы без данных. Сначала диагностика, потом исправления.

